language: cpp
sudo: false
before_install:
    - if [ "$CXX" = "g++" ]; then apt-get install -qq g++-4.8; fi
    - if [ "$CXX" = "g++" ]; then export CXX="g++-4.8" CC="gcc-4.8"; fi

script:
    - cd uv4l2
    - make
